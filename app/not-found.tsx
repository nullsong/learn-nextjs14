import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Not Found',
}
export default function NotFount() {
    return (
        <div>
            <h1>NOT FOUND !</h1>
        </div>

    )

}