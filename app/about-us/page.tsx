import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'About Us',
}

export default function AboutUs() {
    return (
        <div>
            <h1>About us!</h1>
        </div>
    );
}